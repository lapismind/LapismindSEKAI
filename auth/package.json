{
  "name": "sekai-auth",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node tests/worker.test.mjs",
    "db:init": "wrangler d1 execute sekai-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute sekai-db --local --file=./schema.sql"
  },
  "dependencies": {
    "@lapismind/lobby-kit": "file:../packages/lobby-kit"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
