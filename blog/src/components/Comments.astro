---
// 评论岛挂载点 —— 服务端算出 pagePath，客户端加载 Vue 岛。
import CommentSection from './CommentSection.vue';
const { pagePath } = Astro.props as { pagePath: string };
---

<CommentSection client:visible pagePath={pagePath} authBaseUrl="https://auth.qmzhj.top" />
