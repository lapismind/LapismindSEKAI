# /about/ 文案重写

**目标：** 把 `/about/` 从 `/init` 时的开发者简历式内容，改成以个人兴趣和朝比奈真冬为主的个人页面；不编造用户的经历或兴趣。

**当前授权：** 用户确认 `.planning/2026-09-24-about-copy/draft.md` 的收紧版，并明确要求部署 `/about/`。已发布至生产。

## 阶段

- [x] 核对已上线卡片版本、现有 `/about/` 文案及各字段的使用范围。
- [x] 确认用户愿意公开的真实兴趣，以及页面结构的取舍。
- [x] 给出完整的页面顺序与逐段中文文案草稿，供用户审阅。
- [x] 按确认的草稿修改 `blog/src/pages/about.astro` 和 `blog/src/data/profile.ts`。
- [x] 构建并核对桌面与手机呈现。
- [x] 按用户授权部署，核对线上页面。

## 实施步骤

1. `profile.ts`：仅保留身份行与简介的新版文字；移除页面不再使用的 `profileStatus`、`profileTech`。
2. `about.astro`：移除旧状态栏、技术栈、重复的游戏入口；把单推卡紧接档案卡；后面新增四项作品卡。游戏与 Mod 卡分别链接 `/projects/`、`/works/`。
3. 复核 `git diff`：确认 `oshi-card` 内容、样式和脚本、看板娘定位均未变。
4. 运行站点构建与静态检查，再用本地浏览器核对桌面、手机和亮暗主题。

## 边界

- 不动已上线单推卡的图片、短句和动效；不动看板娘定位。
- 删除或替换会过期的硬编码状态，不用新的猜测替代旧的猜测。
- 不把私有个人资料自动发布到公开页面。
