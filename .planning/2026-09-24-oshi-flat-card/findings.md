# 发现

- 用户确认的两张图是昼夜 1:1 扁平画风：人物在右侧，水线约在画面高度 30%，左侧水域已无鱼与造景，符合「水草和沙子」的空缸意象。
- 夜图左上角有一枚小月亮，排版需要绕开；昼夜图片上方均有深蓝色安静区域。
- 现有卡片用 `blog/src/pages/about.astro` 中的 `oshi-day.webp` / `oshi-night.webp`；图片、文字、动效都在同一组件内。现有文案含八项官方资料、三项内在答案与签名句，需要按已确认设计缩减。
- 当前工作区已有 `blog/docs/lessons-learned.md` 未提交改动，属于本会话先前按规则记录的内容；保留它。
- 两个用户提供的临时 PNG 均为 1024×1024，夜图 1,131,770 字节，昼图 1,272,702 字节。`blog/node_modules/sharp` 存在；全局 PATH 中没有 `magick`、`cwebp`、`ffmpeg`。
- 现有 `.oshi-water` 从旧图的 25.76% 高度开始；新图水线约在 30%，动效层和波线位置需要随素材重算。旧移动规则把八项资料和夜间文案整体移到图下，需要替换成方形卡内布局。
- 新图原始 PNG 已留存为 `oshi-day-flat.png` / `oshi-night-flat.png`，Sharp q90 编码后的 WebP 分别为 102,148 / 74,688 字节；肉眼查看编码版与上传图一致。
- 换图后首次 `npm run build` 退出码 0，17 页构建成功；Astro 为新图生成 380/560/760/1024 响应式产物。当前仍是旧文字版式，下一阶段处理。
- 新排版在构建产物的 1200/390/320px 视口、亮暗两种主题均已截图复核。卡片宽度分别为 760/350/280px，始终 1:1。最窄视口公开文字在卡内 y=15–77px，水线约从 y=84px 开始；夜间水下文字透明度为 1，昼间为 0。
- 首轮截图揭露全局 `h3` 颜色覆盖姓名，以及旧样式中多余 `}` 让 `.oshi-waterline` 基础规则丢失；均已修复。水面高光现在有实际尺寸，五条水线、波光、气泡与夜间文字缓漂继续使用原动效。
- 最终 `npm run build`、`npm run check`（0 errors；另有与本次无关的两条 lobby-kit 类型提示）、`npm run lint`、`git diff --check` 均退出码 0。浏览器截图保存在 `docs/agent/scripts/out/oshi-flat-2026-09-24/`（gitignored）。未运行测试，未提交或部署。
