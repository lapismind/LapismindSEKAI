# 进度

## 2026-09-24

- 已读仓库和 blog 项目约定、接力记录；用户已确认新图片和精简排版方向。
- 已核对两个临时 PNG 文件存在；正在检查尺寸、编码方式及当前 CSS，尚未修改卡片。
- 当前 `git status --short`：`M blog/docs/lessons-learned.md`（本会话之前的踩坑记录）。
- 阶段 1 完成：两图尺寸与现有源码、移动布局已核对；确认使用项目现成 Sharp 编码 WebP。下一步保存新图并接入。
- 阶段 2 完成：保存两份原始 PNG 与两份 WebP，新 WebP 已接入 `about.astro` 并更新图片描述；`npm run build` 成功（17 页）。旧版 WebP 保留未动。
- 阶段 3 完成：卡片保持 1:1，手机端不再追加资料尾栏；画面上方仅保留所属、姓名、趣味和喜欢，夜间水下仅保留两条呼应。旧的资料线、六项资料、第三句、签名句及其样式已移除。水线、波光、滚动弹簧、点击气泡和文字缓漂继续保留。
- 阶段 4 完成：构建产物经 Playwright 在 1200/390/320px 的昼夜组合截图，修复姓名颜色、水面高光失效和窄屏文字重叠；末轮六张截图均可读且保持方形。
- 阶段 5 完成：`npm run build`、`npm run check`、`npm run lint`、`git diff --check` 均成功；已确认只修改单推卡页面、对应数据、版本化图片、复核脚本与过程记录，旧图未删。工作区保持未提交，线上未部署。
- 后续字体方向：透明字样尝试得到的 `image (5).png` 实为 RGB 文件、棋盘格是画进图的像素。用户改为让 Images 2.5 分别在昼夜原图上只写「朝比奈真冬」和 `ASAHINA MAFUYU`；所属、两条公开信息与夜间水下文字仍由网页排版，以保留夜间渐显。用户用自己的账号生成，Codex 不调用生图工具。待用户提供两张加字图后接入、移除网页姓名并复核。
- 新版昼夜加字图已由用户确认，原始 PNG 和 1024² WebP 已保存为 `oshi-{day,night}-lettered`。页面已切换图片引用，移除重复的可见姓名/所属，保留屏幕阅读器标题。新名字占满空气层；公开短句的最终位置待用户选择，当前尚未完成排版与视觉复核。
- 按推荐方案做了可审阅排版：两组短句在左侧空水区同位昼夜切换，标签细小、答案稍大，切主题时错拍淡入淡出；窄卡收紧行距。无 JS 时用卡外 `<noscript>` 提供夜间两句。保留原有水线、波光、滚动弹簧、点击气泡与夜间微漂。
- `npm run build`、`npm run check`（0 errors、2 处既有 hints）、`npm run lint`、`git diff --check` 成功；Playwright 视觉截图见 `docs/agent/scripts/out/oshi-flat-2026-09-24/`，760/350/280px 昼夜六档均已人工查看。尚待用户审阅这版短句布局；未提交、未部署。
- 用户要求用样例网页看效果：样例源文件存于本目录 `demo.html`，当前复制到 `blog/dist/oshi-demo/index.html`，本地 `http://127.0.0.1:8766/oshi-demo/` 可切昼夜与桌面/手机/窄屏，并复用 `/about/` 的实际卡片和动效。样例不进生产路由；重建 `blog/dist/` 后需重新复制。初版演示因懒加载出现底色，已在 iframe 内把图片设为 eager，复核三张演示截图均正常。
- 用户看过样例网页并授权部署。2026-09-24 生产构建 17 页成功，`npm run check` 0 errors / 2 处既有 hints，`npm run lint` 与 `git diff --check` 成功；`blog/dist/oshi-demo/` 已被构建清除，样例未进入部署包。Wrangler 部署到 `blog.qmzhj.top`，Version ID `cc137257-4eb9-4f75-9146-82585fc415e9`。线上 `/about/` 昼夜均 HTTP 200，分别加载 `oshi-day-lettered` / `oshi-night-lettered`，公开/隐藏短句透明度为 1/0 和 0/1，截图已人工核对。下一轮是重写 `/about/` 本身的自我介绍文案。
